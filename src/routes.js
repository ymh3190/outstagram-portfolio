const HOME = "/";
const SEARCH = "/search";
const USER = "/user";
const POST = "/post";
const ACTIVITY = "/activity";

const routes = {
  home: HOME,
  search: SEARCH,
  user: USER,
  post: POST,
  activity: ACTIVITY,
};

export default routes;
